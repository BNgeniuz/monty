#include "monty.h"

/**
 * mo_stack - prints the top
 * @master: head input
 * @reader: line read
 * Return: 0 (success)
*/
void mo_stack(stack_t **master, unsigned int reader)
{
	(void)master;
	(void)reader;
	bus.lifi = 0;
}
